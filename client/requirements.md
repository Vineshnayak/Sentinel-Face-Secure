## Packages
react-webcam | For capturing face images from the user's camera
framer-motion | For smooth animations and scanning effects
date-fns | For formatting dates in logs

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
